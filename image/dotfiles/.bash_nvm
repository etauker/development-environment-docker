#!/bin/bash
export NVM_DIR="$HOME/.nvm"
echo "NVM_DIR: $NVM_DIR"
[[ -s "$NVM_DIR/nvm.sh" ]] && source "$NVM_DIR/nvm.sh"
[[ -s "$NVM_DIR/bash_completion" ]] && source "$NVM_DIR/bash_completion"
nvm ls-remote
nvm install v10.16.0


echo '(OK) bash nvm sourced successfully'
